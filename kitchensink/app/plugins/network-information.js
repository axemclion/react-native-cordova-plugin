import React, {Component} from 'react-native';

export default class NetworkInfo extends Component {
    render() {
        return (
            <View>Network Information Plugin</View>
        )
    }
}